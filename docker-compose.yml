services:
  postgres:
    image: postgres:17-alpine
    command: postgres -c max_connections=200
    environment:
      POSTGRES_USER: concerto
      POSTGRES_PASSWORD: concerto
      POSTGRES_DB: concerto
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    image: oven/bun:slim
    working_dir: /app
    volumes:
      - .:/app
      - app_node_modules:/app/node_modules
    command: sh -c "bun install && bun run dev -- --hostname 0.0.0.0 --turbopack"
    ports:
      - "3000:3000"
    environment:
      RTK_ORG_ID: "${RTK_ORG_ID:-}"
      RTK_API_KEY: "${RTK_API_KEY:-}"
      # RTK_ACCOUNT_ID, RTK_APP_ID, CLOUDFLARE_API_TOKEN are intentionally NOT set here
      # so that Next.js can read them from .env.local (mounted via volumes: - .:/app)
      DATABASE_URL: "postgresql://concerto:concerto@postgres:5432/concerto"
      BETTER_AUTH_SECRET: "dev-secret-change-in-production"
      BETTER_AUTH_URL: "http://127.0.0.1:3000"
      HOST: "0.0.0.0"
      TRUSTED_ORIGINS: "${TRUSTED_ORIGINS:-}"
      R2_ACCOUNT_ID: "${R2_ACCOUNT_ID:-}"
      R2_ACCESS_KEY_ID: "${R2_ACCESS_KEY_ID:-}"
      R2_SECRET_ACCESS_KEY: "${R2_SECRET_ACCESS_KEY:-}"
      R2_BUCKET_NAME: "${R2_BUCKET_NAME:-concerto-recordings}"
      R2_PUBLIC_URL: "${R2_PUBLIC_URL:-}"
      # S3_* vars are NOT set here so that Next.js reads them from .env.local (mounted via volumes)
    depends_on:
      - postgres

volumes:
  app_node_modules:
  pgdata:
